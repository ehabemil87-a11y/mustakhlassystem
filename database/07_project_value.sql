-- =====================================================================
--  07 — إجمالي قيمة المشروع (لحساب المحصّل والمتبقي)
--  ينفذ بعد 06
--
--  «المحصّل» لكل مشروع = مجموع قيم مستخلصاته.
--  «المتبقي» = contract_value − المحصّل. (يُحسب في الواجهة)
-- =====================================================================

alter table public.projects
  add column if not exists contract_value numeric(14,2) not null default 0;
